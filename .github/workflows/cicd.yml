name: my first pipeline

on:
    push:
        branches: ["main"]
    
jobs:
    build:
      runs-on: ubuntu-latest #macos, windows-latest
      steps:
       - name: Clone the code
         uses: actions/checkout@v3
       - name: Build image
         run:
          docker build -t lhenry99/cidc-portfolio .
       - name: Docker login
         uses: docker/login-action@v3
         with:
           username: lhenry99
           password: ${{ secrets.DOCKER_PASSWORD }}
       - name: push image
         run:
            docker push lhenry99/cidc-portfolio





    

            



                    




            
                


        
    

    